{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "salto_monorepo",
  "devDependencies": {
    "codecov": "^3.5.0"
  },
  "scripts": {
    "install": "for WS in @salto/lowerdash @salto/dag adapter-api salesforce-adapter salto salto-vscode @salto/cli; do yarn workspace $WS run build || exit; done",
    "lint-fix": "yarn workspaces run lint-fix"
  }
}
