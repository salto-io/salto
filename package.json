{
  "private": true,
  "workspaces": {
    "packages": ["packages/*"],
    "nohoist": ["salto-vscode/**"]
  },
  "name": "salto_monorepo",
  "devDependencies": {
    "codecov": "^3.5.0"
  },
  "scripts": {
    "install": "./build_utils/wstopo/wstopo run-parallel",
    "test": "yarn run jest --forceExit --detectOpenHandles"
  }
}
