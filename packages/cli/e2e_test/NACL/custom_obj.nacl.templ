type salesforce.${objName} {
  annotations {
    serviceid metadataType {
    }
  }
  metadataType = "CustomObject"
  salesforce.Text Alpha {
    label = "${alphaLabel}"
    _required = false
  }
  salesforce.Text Beta {
    label = "${betaLabel}"
    _required = false
  }
}