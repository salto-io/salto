#!/usr/bin/env node --experimental-exports
require('source-map-support').install()
const cli = require('../dist/src/cli/cli').default

const {
  stdin, stdout, stderr, argv,
} = process

const args = argv.slice(2)

cli(
  { args, stdin },
  { stdout, stderr },
).then(exitCode => process.exit(exitCode))
