# Zendesk Support CLI options

## Non interactive Login Parameters
Supprted parameters are:
* `username`
* `password`
* `subdomain`

### Example
```
salto service add zendesk_support --login-parameters username=SomeUsername password=SomePasswd subdomain=SomeSubdomain
```
